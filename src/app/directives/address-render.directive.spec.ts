import { AddressRenderDirective } from './address-render.directive';

describe('AddressRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new AddressRenderDirective();
    expect(directive).toBeTruthy();
  });
});
